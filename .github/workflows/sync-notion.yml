name: Sync Notion to Supabase

on:
  schedule:
    # 每 3 小时执行一次（UTC 时间）
    - cron: '0 */3 * * *'

  push:
    branches:
      - main

  workflow_dispatch:
    # 支持手动触发
    inputs:
      days:
        description: '同步最近多少天的页面（留空则使用默认的3小时）'
        required: false
        type: string
        default: ''

jobs:
  sync:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Build project
        run: bun run build

      - name: Calculate time range
        id: time
        run: |
          # 获取当前 UTC 时间戳（秒）
          UTC_TIMESTAMP=$(date -u +%s)

          # 加 8 小时（28800 秒）得到北京时间戳
          BEIJING_TIMESTAMP=$((UTC_TIMESTAMP + 28800))

          # 转换为北京时间格式
          END_TIME=$(date -u -d "@$BEIJING_TIMESTAMP" +"%Y%m%d%H%M%S")

          # 判断是手动触发且有输入天数，还是定时触发（默认3小时）
          DAYS="${{ github.event.inputs.days }}"

          if [ -n "$DAYS" ] && [ "$DAYS" != "0" ]; then
            # 手动触发：同步最近 N 天
            SECONDS_AGO=$((DAYS * 86400))  # 天数转换为秒（1天 = 86400秒）
            START_TIMESTAMP=$((BEIJING_TIMESTAMP - SECONDS_AGO))
            START_TIME=$(date -u -d "@$START_TIMESTAMP" +"%Y%m%d%H%M%S")
            echo "📅 手动触发：同步最近 $DAYS 天的页面"
          else
            # 定时触发或手动触发但未输入天数：默认 3 小时
            START_TIMESTAMP=$((BEIJING_TIMESTAMP - 10800))  # 3小时 = 10800秒
            START_TIME=$(date -u -d "@$START_TIMESTAMP" +"%Y%m%d%H%M%S")
            echo "⏰ 定时触发：同步最近 3 小时的页面"
          fi

          echo "start_time=$START_TIME" >> $GITHUB_OUTPUT
          echo "end_time=$END_TIME" >> $GITHUB_OUTPUT

          echo "Time range (Beijing Time): $START_TIME to $END_TIME"

      - name: Sync Notion databases
        id: sync
        env:
          NOTION_API_KEY: ${{ secrets.NOTION_API_KEY }}
          SUPABASE_URL: ${{ vars.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          ZILEAN_CLOUDFLARE_R2_TOKEN: ${{ secrets.ZILEAN_CLOUDFLARE_R2_TOKEN }}
          NOTION_DATABASE_IDS: ${{ vars.NOTION_DATABASE_IDS }}
        run: |
          # 从环境变量读取数据库 ID 列表（逗号分隔）
          # 例如：NOTION_DATABASE_IDS="id1,id2,id3"

          if [ -z "$NOTION_DATABASE_IDS" ]; then
            echo "::error::NOTION_DATABASE_IDS 未配置"
            exit 1
          fi

          # 将逗号分隔的字符串转换为数组
          IFS=',' read -ra DATABASES <<< "$NOTION_DATABASE_IDS"

          START_TIME="${{ steps.time.outputs.start_time }}"
          END_TIME="${{ steps.time.outputs.end_time }}"

          echo "=========================================="
          echo "开始同步 Notion 数据库"
          echo "时间范围: $START_TIME - $END_TIME"
          echo "数据库数量: ${#DATABASES[@]}"
          echo "=========================================="
          echo ""

          # 统计变量
          TOTAL_DBS=${#DATABASES[@]}
          SUCCESS_COUNT=0
          FAIL_COUNT=0
          FAILED_DBS=()
          HAS_UPDATES=false

          # 遍历所有数据库
          INDEX=1
          for DB_ID in "${DATABASES[@]}"; do
            # 去除空格
            DB_ID=$(echo "$DB_ID" | xargs)

            if [ -z "$DB_ID" ]; then
              continue
            fi

            echo "=========================================="
            echo "[$INDEX/$TOTAL_DBS] 同步数据库"
            echo "数据库 ID: $DB_ID"
            echo "=========================================="

            # 执行同步命令并捕获输出
            SYNC_OUTPUT=$(bun run dist/index.js sync-updated-page "$DB_ID" "$START_TIME" "$END_TIME" 2>&1)
            SYNC_EXIT_CODE=$?

            echo "$SYNC_OUTPUT"

            if [ $SYNC_EXIT_CODE -eq 0 ]; then
              echo "✅ 数据库同步成功"
              SUCCESS_COUNT=$((SUCCESS_COUNT + 1))

              # 检查是否有数据更新
              if echo "$SYNC_OUTPUT" | grep -q "has_updates::true"; then
                HAS_UPDATES=true
              fi
            else
              echo "❌ 数据库同步失败"
              FAIL_COUNT=$((FAIL_COUNT + 1))
              FAILED_DBS+=("$DB_ID")
            fi

            echo ""
            INDEX=$((INDEX + 1))
          done

          # 输出最终统计
          echo "=========================================="
          echo "同步完成统计"
          echo "=========================================="
          echo "总数据库数: $TOTAL_DBS"
          echo "成功: $SUCCESS_COUNT"
          echo "失败: $FAIL_COUNT"

          # 如果有失败的数据库，列出详情
          if [ $FAIL_COUNT -gt 0 ]; then
            echo ""
            echo "失败的数据库:"
            for FAILED_DB in "${FAILED_DBS[@]}"; do
              echo "  - $FAILED_DB"
            done
            echo ""
            echo "=========================================="
            exit 1
          fi

          echo "=========================================="
          echo "✅ 所有数据库同步成功！"
          echo "=========================================="

          # 设置输出变量
          echo "has_updates=$HAS_UPDATES" >> $GITHUB_OUTPUT

      - name: Notify zilean to update submodule (data updated)
        if: steps.sync.outputs.has_updates == 'true'
        run: |
          echo "🔔 检测到数据更新，通知 zilean 项目更新子模块..."
          curl -X POST https://api.github.com/repos/vitahlin/zilean/dispatches \
            -H "Authorization: token ${{ secrets.ZILEAN_AND_ANIVIA_GIT_TOKEN }}" \
            -H "Accept: application/vnd.github.v3+json" \
            -d '{"event_type":"update_submodule_anivia"}'
          echo "✅ 通知已发送"

      - name: Notify zilean to update submodule (main push)
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          echo "🔔 检测到 main 分支有新提交，通知 zilean 项目更新子模块..."
          curl -X POST https://api.github.com/repos/vitahlin/zilean/dispatches \
            -H "Authorization: token ${{ secrets.ZILEAN_AND_ANIVIA_GIT_TOKEN }}" \
            -H "Accept: application/vnd.github.v3+json" \
            -d '{"event_type":"update_submodule_anivia"}'
          echo "✅ 通知已发送"

      - name: Notify on failure
        if: failure()
        run: |
          echo "::error::Notion 同步失败，请检查日志"

